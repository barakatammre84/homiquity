{
  "$comment": "Shared project instructions are in AGENTS.md. This file configures hooks; per-machine permissions remain in the untracked .claude/settings.local.json. SessionStart prints the shared instruction pointer and verifies/materializes the pinned Selling Guide corpus. It does not establish legal requirements or prove the corpus is the latest applicable edition. Hook behavior is unchanged by the direction cleanup.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"$CLAUDE_PROJECT_DIR/scripts/selling-guide-session-hook.cjs\"",
            "timeout": 240
          }
        ]
      }
    ]
  }
}
