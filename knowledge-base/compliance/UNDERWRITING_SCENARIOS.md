# Implemented scenario inventory

This is a code-coverage index for `server/services/scenarioCatalog.ts` and its tests, not a source
of underwriting policy. Verify each substantive rule against an applicable primary source when
using or changing it. The old narrative and backlog are recoverable from Git.

## Implemented

### S-01: Hybrid W-2 / Self-Employed Creator (income seasoning)

Implementation: `server/services/scenarioCatalog.ts`, entry `S-01`.

### S-02: Relocating Military Veteran (VA residual income)

Implementation: `server/services/scenarioCatalog.ts`, entry `S-02`.

### S-03: The "Sleeper Debt" Trap (undisclosed liabilities)

Implementation: `server/services/scenarioCatalog.ts`, entry `S-03`.

### S-04: The "Mattress Money" Gift Fund (large-deposit sourcing)

Implementation: `server/services/scenarioCatalog.ts`, entry `S-04`.

### S-05: Rental Income Calculation (Schedule E)

Implementation: `server/services/scenarioCatalog.ts`, entry `S-05`.

### S-06: Multi-Unit Subject Property Rental Income

Implementation: `server/services/scenarioCatalog.ts`, entry `S-06`.

### S-07: Rental Income Conversion (departing residence)

Implementation: `server/services/scenarioCatalog.ts`, entry `S-07`.

## Backlog

Use current product direction and issues.
