# selling-guide source inventory

Navigation only. Read the applicable primary artifact and verify its version, provenance and
applicability using the [source map](../../../knowledge-base/compliance/SELLING_GUIDE_DECISION_RULE.md).
A captured file is not proof that it is current or that its contents apply to this broker.

The tracked manifest, section index and TOC describe the pinned capture. The full copyrighted
text and PDF are materialized locally with `python3 scripts/extract-selling-guide.py`;
`python3 scripts/extract-selling-guide.py --section B3-6-05 --markdown` locates one section.
Confirm table values on the PDF page. Extraction integrity is checked by the existing corpus guard.

## Tracked artifacts

- `links.json`
- `manifest.json`
- `revised-sections.tsv`
- `section-index.tsv`
- `toc.json`
